namespace ModularisTest
{
    public enum MessageType
    {
        Message,
        Warning,
        Error
    }

    public enum LogDestination
    {
        Console,
        TextFile,
        Database
    }
}
